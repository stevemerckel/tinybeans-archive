namespace TBA.Tests
{
    /// <summary>
    /// List of possible test categories
    /// </summary>
    public static class TestCategories
    {
        /// <summary>
        /// Use when the specific test might only work for the 
        /// </summary>
        public const string AccountSpecific = nameof(AccountSpecific);
    }
}